# 🚀 Пошаговая инструкция по развертыванию

## ШАГ 1: ПОДГОТОВКА ФРОНТЕНДА

### 1.1. Создайте файлы переменных окружения

```bash
cd client
```

Создайте файл `.env`:
```env
VITE_API_URL=http://localhost:3000/api
VITE_TELEGRAM_BOT_TOKEN=your_bot_token_here
```

Создайте файл `.env.production`:
```env
VITE_API_URL=https://your-backend-domain.com/api
VITE_TELEGRAM_BOT_TOKEN=your_bot_token_here
```

### 1.2. Установите зависимости (если еще не установлены)

```bash
npm install
```

### 1.3. Проверьте сборку

```bash
npm run build
```

---

## ШАГ 2: СОЗДАНИЕ И ЗАПУСК БЭКЕНДА

### 2.1. Перейдите в папку server

```bash
cd ../server
```

### 2.2. Установите зависимости

```bash
npm install
```

### 2.3. Создайте файл .env

```bash
# Скопируйте пример
cp .env.example .env
```

Отредактируйте `.env`:
```env
PORT=3000
NODE_ENV=development
TELEGRAM_BOT_TOKEN=your_bot_token_here
JWT_SECRET=your_jwt_secret_here_change_in_production
CORS_ORIGIN=http://localhost:5173
```

### 2.4. Запустите бэкенд в режиме разработки

```bash
npm run dev
```

Бэкенд должен запуститься на `http://localhost:3000`

### 2.5. Проверьте работу API

Откройте в браузере: `http://localhost:3000/health`

Должен вернуться JSON: `{"status":"ok","timestamp":"..."}`

---

## ШАГ 3: ИНТЕГРАЦИЯ ФРОНТЕНДА И БЭКЕНДА

### 3.1. Запустите фронтенд

В новом терминале:
```bash
cd client
npm run dev
```

### 3.2. Проверьте работу

Откройте `http://localhost:5173` и проверьте:
- Создание целей
- Сохранение данных
- Работу с караванами

---

## ШАГ 4: НАСТРОЙКА TELEGRAM MINI APP

### 4.1. Создайте бота через BotFather

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте команду `/newbot`
4. Следуйте инструкциям:
   - Введите имя бота (например: "Road To Your Dream")
   - Введите username бота (например: "road_to_dream_bot")
5. Сохраните токен бота (выглядит как: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 4.2. Создайте Web App

1. Отправьте BotFather команду `/newapp`
2. Выберите вашего бота
3. Заполните данные:
   - **Title**: Road To Your Dream
   - **Short name**: roadtodream (будет использоваться в URL)
   - **Description**: Достигай своих целей вместе с нами
   - **Photo**: Загрузите иконку 512x512px
   - **Web App URL**: Пока оставьте пустым (заполните после деплоя)

### 4.3. Обновите токен в .env файлах

В `server/.env`:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
```

В `client/.env` и `client/.env.production`:
```env
VITE_TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
```

---

## ШАГ 5: ДЕПЛОЙ

### Вариант A: Деплой на Vercel (Фронтенд) + Railway (Бэкенд)

#### 5A.1. Деплой фронтенда на Vercel

1. Установите Vercel CLI:
```bash
npm i -g vercel
```

2. Войдите в Vercel:
```bash
vercel login
```

3. Деплой:
```bash
cd client
vercel
```

4. Добавьте переменные окружения в Vercel Dashboard:
   - `VITE_API_URL` = URL вашего бэкенда
   - `VITE_TELEGRAM_BOT_TOKEN` = токен бота

#### 5A.2. Деплой бэкенда на Railway

1. Зарегистрируйтесь на [Railway.app](https://railway.app)
2. Создайте новый проект
3. Подключите GitHub репозиторий или загрузите папку `server`
4. Добавьте переменные окружения:
   - `PORT` = 3000
   - `NODE_ENV` = production
   - `TELEGRAM_BOT_TOKEN` = ваш токен
   - `JWT_SECRET` = случайная строка
   - `CORS_ORIGIN` = URL вашего фронтенда на Vercel
5. Railway автоматически определит Node.js проект и запустит его

### Вариант B: Деплой на Netlify (Фронтенд) + Render (Бэкенд)

#### 5B.1. Деплой фронтенда на Netlify

1. Зарегистрируйтесь на [Netlify](https://netlify.com)
2. Создайте новый сайт
3. Подключите GitHub репозиторий
4. Настройки сборки:
   - **Build command**: `cd client && npm install && npm run build`
   - **Publish directory**: `client/dist`
5. Добавьте переменные окружения в Netlify Dashboard

#### 5B.2. Деплой бэкенда на Render

1. Зарегистрируйтесь на [Render.com](https://render.com)
2. Создайте новый Web Service
3. Подключите GitHub репозиторий
4. Настройки:
   - **Root Directory**: `server`
   - **Build Command**: `npm install && npm run build`
   - **Start Command**: `npm start`
5. Добавьте переменные окружения

### Вариант C: Деплой на VPS (оба на одном сервере)

#### 5C.1. Подготовка VPS

```bash
# Обновите систему
sudo apt update && sudo apt upgrade -y

# Установите Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# Установите Nginx
sudo apt install -y nginx

# Установите PM2 для управления процессами
sudo npm install -g pm2
```

#### 5C.2. Деплой бэкенда

```bash
# Скопируйте папку server на сервер
scp -r server user@your-server-ip:/home/user/

# Подключитесь к серверу
ssh user@your-server-ip

# Перейдите в папку server
cd server

# Установите зависимости
npm install

# Создайте .env файл
nano .env
# Добавьте все переменные окружения

# Соберите проект
npm run build

# Запустите с PM2
pm2 start dist/index.js --name "road-to-dream-api"
pm2 save
pm2 startup
```

#### 5C.3. Деплой фронтенда

```bash
# На вашем компьютере
cd client
npm run build

# Скопируйте dist на сервер
scp -r dist user@your-server-ip:/var/www/road-to-dream/

# На сервере настройте Nginx
sudo nano /etc/nginx/sites-available/road-to-dream
```

Конфигурация Nginx:
```nginx
server {
    listen 80;
    server_name your-domain.com;

    # Frontend
    location / {
        root /var/www/road-to-dream;
        try_files $uri $uri/ /index.html;
    }

    # Backend API
    location /api {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

```bash
# Активируйте конфигурацию
sudo ln -s /etc/nginx/sites-available/road-to-dream /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

#### 5C.4. Настройка SSL (Let's Encrypt)

```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

---

## ШАГ 6: ФИНАЛЬНАЯ НАСТРОЙКА TELEGRAM

### 6.1. Обновите Web App URL

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/myapps`
3. Выберите ваше приложение
4. Выберите "Edit Web App URL"
5. Введите URL вашего фронтенда (например: `https://your-app.vercel.app`)

### 6.2. Создайте команду /start

Отправьте BotFather:
```
/setcommands
```

Выберите вашего бота и добавьте:
```
start - Начать работу с приложением
```

### 6.3. Добавьте кнопку меню

Отправьте BotFather:
```
/setmenubutton
```

Выберите вашего бота и настройте кнопку меню.

---

## ШАГ 7: ТЕСТИРОВАНИЕ

### 7.1. Локальное тестирование

1. Запустите бэкенд: `cd server && npm run dev`
2. Запустите фронтенд: `cd client && npm run dev`
3. Откройте `http://localhost:5173`
4. Проверьте все функции

### 7.2. Тестирование в Telegram

1. Откройте вашего бота в Telegram
2. Нажмите на кнопку меню или отправьте `/start`
3. Проверьте:
   - ✅ Создание целей
   - ✅ Сохранение прогресса
   - ✅ Работу с караванами
   - ✅ Профиль и статистику
   - ✅ Магазин

---

## 🐛 РЕШЕНИЕ ПРОБЛЕМ

### Проблема: CORS ошибки

**Решение**: Убедитесь, что `CORS_ORIGIN` в `.env` бэкенда совпадает с URL фронтенда.

### Проблема: Telegram authentication не работает

**Решение**: 
1. Проверьте, что токен бота правильный
2. Убедитесь, что скрипт `telegram-web-app.js` загружается в `index.html`
3. Проверьте, что приложение открывается через Telegram, а не напрямую в браузере

### Проблема: Данные не сохраняются

**Решение**:
1. Проверьте, что бэкенд запущен и доступен
2. Проверьте переменную `VITE_API_URL` во фронтенде
3. Проверьте консоль браузера на наличие ошибок

---

## 📝 ЧЕКЛИСТ ПЕРЕД ЗАПУСКОМ

- [ ] Бэкенд запущен и доступен
- [ ] Фронтенд собран и задеплоен
- [ ] Переменные окружения настроены
- [ ] Telegram бот создан и настроен
- [ ] Web App URL указан в BotFather
- [ ] SSL сертификат установлен (для продакшена)
- [ ] Все функции протестированы

---

## 🎉 ГОТОВО!

Ваше приложение должно работать! Если возникнут проблемы, проверьте логи:
- Бэкенд: `pm2 logs` (на VPS) или логи в Railway/Render
- Фронтенд: консоль браузера (F12)

