# 🔧 Исправление ошибки "Cannot GET /"

## Проблема
При открытии `http://localhost:3000` появлялась ошибка "Cannot GET /" потому что не был определен маршрут для корневого пути.

## ✅ Решение
Добавлен маршрут для корневого пути "/" в `server/src/index.ts`.

## 🚀 Что делать:

1. **Остановите текущий сервер** (если он запущен):
   - Нажмите `Ctrl+C` в терминале, где запущен сервер

2. **Перезапустите сервер:**
   ```bash
   cd server
   npm run dev
   ```

3. **Проверьте:**
   - Откройте `http://localhost:3000` - теперь должно показать информацию об API
   - Откройте `http://localhost:3000/health` - должно показать `{"status":"ok",...}`

## 📋 Доступные эндпоинты:

- `GET /` - Информация об API
- `GET /health` - Проверка здоровья сервера
- `GET /api/goals` - Все цели
- `GET /api/users/me` - Текущий пользователь
- `GET /api/caravans` - Все караваны

---

**После перезапуска всё должно работать! ✅**

