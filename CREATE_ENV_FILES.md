# 📝 Создание файлов .env

## 📍 Где находятся файлы .env

Файлы `.env` должны быть созданы в следующих местах:

1. **`server/.env`** - для бэкенда
2. **`client/.env`** - для фронтенда

## 🚀 Быстрое создание

### Вариант 1: Через командную строку (Windows PowerShell)

```powershell
# Перейдите в корень проекта
cd "c:\Users\upgra\Downloads\stitch_welcome_screen (1)\stitch_welcome_screen"

# Создайте server/.env из примера
Copy-Item "server\.env.example" "server\.env"

# Создайте client/.env из примера
Copy-Item "client\.env.example" "client\.env"
```

### Вариант 2: Вручную

1. **Создайте `server/.env`**:
   - Откройте папку `server`
   - Скопируйте файл `.env.example`
   - Переименуйте в `.env`
   - Отредактируйте и замените `your_bot_token_here` на ваш токен

2. **Создайте `client/.env`**:
   - Откройте папку `client`
   - Скопируйте файл `.env.example`
   - Переименуйте в `.env`
   - Отредактируйте и замените `your_bot_token_here` на ваш токен

## 📋 Содержимое файлов

### `server/.env`
```env
PORT=3000
NODE_ENV=development
TELEGRAM_BOT_TOKEN=your_bot_token_here
JWT_SECRET=your_jwt_secret_here_change_in_production
CORS_ORIGIN=http://localhost:5173
```

### `client/.env`
```env
VITE_API_URL=http://localhost:3000/api
VITE_TELEGRAM_BOT_TOKEN=your_bot_token_here
```

## ⚠️ Важно

1. **Не коммитьте `.env` файлы в Git!** Они уже в `.gitignore`
2. **Замените `your_bot_token_here`** на реальный токен от [@BotFather](https://t.me/BotFather)
3. **Для продакшена** создайте отдельные `.env.production` файлы с другими значениями

## 🔑 Как получить токен бота

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте `/newbot`
4. Следуйте инструкциям
5. Скопируйте полученный токен (выглядит как: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## ✅ Проверка

После создания файлов проверьте:

```powershell
# Проверьте наличие файлов
Test-Path "server\.env"  # Должно вернуть True
Test-Path "client\.env"  # Должно вернуть True
```

---

**Готово! Теперь можно запускать проект.** 🚀

