# 🤖 Настройка Telegram бота с приветственным сообщением

## Что было добавлено:

1. **Telegram бот** с обработкой команды `/start`
2. **Приветственное сообщение** с мотивирующим текстом
3. **Кнопка "НАЧНИ СВОЮ ДОРОГУ К ЦЕЛЯМ"** которая открывает Web App
4. **Поддержка изображения** в приветственном сообщении

## 📋 Шаги настройки:

### 1. Установите зависимости

```bash
cd server
npm install
```

Это установит библиотеку `node-telegram-bot-api` для работы с Telegram Bot API.

### 2. Добавьте переменные окружения

В `server/.env` добавьте:

```env
TELEGRAM_BOT_TOKEN=8296730382:AAFubPJjekcbZvQTAcnYUt0NR_3vRdfjQjg
WEB_APP_URL=https://ваш-фронтенд.vercel.app
```

**Примечание:** `WEB_APP_URL` можно не указывать, если указан `CORS_ORIGIN` - бот будет использовать его.

### 3. Для Railway (продакшн)

В Railway → Variables добавьте:

```
WEB_APP_URL=https://ваш-фронтенд.vercel.app
```

Или убедитесь, что `CORS_ORIGIN` установлен на URL фронтенда.

### 4. Запустите сервер

```bash
npm run dev
```

Бот автоматически запустится и начнет обрабатывать команды.

### 5. Проверьте работу

1. Откройте вашего бота в Telegram
2. Отправьте команду `/start`
3. Вы должны увидеть:
   - Красивое изображение (или текст, если изображение не загрузилось)
   - Мотивирующее приветственное сообщение
   - Кнопку "🚀 НАЧНИ СВОЮ ДОРОГУ К ЦЕЛЯМ"
4. Нажмите на кнопку - должно открыться ваше приложение

## 🎨 Настройка изображения

По умолчанию используется изображение из Unsplash. Чтобы использовать свое изображение:

1. Загрузите изображение на хостинг (например, в папку `client/public/`)
2. После деплоя получите публичный URL
3. В `server/src/bot.ts` замените `photoUrl`:

```typescript
const photoUrl = 'https://ваш-домен.com/path/to/image.jpg';
```

**Рекомендации для изображения:**
- Размер: 800x600px или больше
- Формат: JPG или PNG
- Тема: мотивация, цели, успех, дорога к мечте

## 📝 Команды бота

- `/start` - Приветственное сообщение с кнопкой
- `/help` - Справка по использованию

## 🔧 Устранение проблем

### Бот не отвечает

1. Проверьте, что `TELEGRAM_BOT_TOKEN` установлен правильно
2. Убедитесь, что сервер запущен
3. Проверьте логи на наличие ошибок

### Изображение не загружается

- Бот автоматически отправит только текст с кнопкой
- Проверьте, что URL изображения доступен публично
- Убедитесь, что URL начинается с `https://`

### Кнопка не работает

- Проверьте, что `WEB_APP_URL` или `CORS_ORIGIN` установлен правильно
- Убедитесь, что URL начинается с `https://`
- Проверьте, что Web App настроен в BotFather

## ✅ Готово!

После настройки ваш бот будет отправлять красивое приветственное сообщение каждому пользователю, который отправит `/start`!

